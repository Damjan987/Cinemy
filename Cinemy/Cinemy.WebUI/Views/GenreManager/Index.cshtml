@model IEnumerable<Cinemy.Core.Models.Genre>

@{
    ViewBag.Title = "Index";
}

<div class="container body-content">
    <h2>Genres</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a href="@Url.Action("Edit", "GenreManager", new { Id=item.Id })" class="btn btn-danger">Edit</a>
                    <a href="@Url.Action("Delete", "GenreManager", new { Id=item.Id })" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }

    </table>
</div>